# Study fav — 표준(단봉) BSS에서 최적화 vs 샘플링 2축 비교 (6샘플러)

> 산출물 위치: `simulation/study_fav/` (git 추적)
> 결과 근거 파일: `redesign_gpu_n*.csv`(샘플링 T=1), `opt_results_difficulty.csv`·
> `opt_results_nsweep_moderate.csv`(최적화 annealing), Notion "재실험 결과" 페이지의
> T=0.01 토글·런타임(#5). 집계: `aggregate_redesign.py`.

## 1. 목적

지도교수의 요구는 **"최적화가 목표"**다. 그런데 BSS의 강점(FDR 통제·불확실성)은
샘플링에서만 나온다. 이 스터디는 두 요구를 하나의 그림으로 합친다.

**같은 데이터·같은 에너지 U(θ)** 위에서 온도만 바꿔 두 축을 동시에 본다.

| 축 | 온도 | 불변분포 | 목표 | 지표 |
|---|---|---|---|---|
| **최적화** | T→0 (annealing) 또는 상수 T=0.01 | 없음(MAP로 수렴) | U 최소화 = MAP/최빈점 | min U / opt.gap / ‖θ−θ*‖ / Tε |
| **샘플링** | T=1 | exp(−U) = 참 사후분포 | 사후분포 재현 | γ-FDR / 순위 / σ̂·Jaccard·cov90 |

온도 T는 최적화와 샘플링을 잇는 **유일한 스위치**다. 데이터도 에너지도 그대로 두고
T=1이면 사후 샘플, T→0이면 MAP 최적화가 된다. 6개 샘플러(tMH·SGLD·qSGLD·cycSGLD·
SGHMC·AWSGLD)를 두 축에서 모두 겨룬다.

**표준 단봉 BSS만 쓴다.** Study 3처럼 log-sum-exp mixture로 인위적 다봉을 만들지 않는다.
문서를 병합할 때도 표준 에너지(데이터 레벨 그래프 병합)를 유지하므로 사후분포는 단봉이고
θ*(gold-free 기준점)가 존재한다. 즉 이 스터디는 **AWSGLD가 다봉 탈출 없이도**
표준 문제에서 경쟁 알고리즘을 이기는지를 본다.

## 2. 설계

### 2.1 에너지 (표준 BSS)

```
U(θ) = −loglik(Y | θ, α) + ‖B(θ − u₀)‖² / (2σ²)
p(θ|Y) ∝ exp(−U(θ))
```

θ = logit score, π = sigmoid(θ) = 키프레이즈 확률, α = PU 라벨노이즈, σ² = prior 분산,
B = I − d·(D⁻¹A)ᵀ (d=0.85), u₀ = B⁻¹(1−d)1. 우도 Hessian은 대각(문서 내 노드별로
분해)이라 U는 **볼록·단봉**이다 (다중 재출발 → 동일 최빈점, mode에서 λ_min(H)>0 확인).

### 2.2 볼록성 → 최적화 기준점 θ*·U*

θ* = GD 최적화 해(u₀에서 8000스텝 gradient descent), U* = U(θ*). 정답 gold 없이도
볼록성 덕에 θ*가 유일하게 정해진다. opt.gap = U − U*, ‖θ−θ*‖로 최적화 품질을 측정한다.

### 2.3 예산 공정성 (= AWSGLD Tε)

최적화 예산(iteration)은 **AWSGLD가 ε-최적(U−U* < ε=1.0)에 도달하는 반복수 Tε의 중앙값**으로
고정하고, 6개 알고리즘 모두 같은 예산을 쓴다. AWSGLD가 가장 먼저 도달하므로 이는 AWSGLD에
불리하지 않은 공정 기준이다. 기본 ~1500, n·문서수 스윕은 ~2000~3000.

### 2.4 지표 정의

- **최적화**: min U(관측된 최저 에너지), opt.gap(U−U*), ‖θ−θ*‖, Tε(ε-도달 반복수)
- **샘플링(gold 필요)**: γ별 FDR 제어(precision/recall/F1/realFDR), 순위(Spearman/Kendall/
  Top-k/NDCG/MSE/AUC)
- **샘플링(gold-free)**: σ̂(사후폭 — 붕괴 탐지), Jaccard(재현성), cov90(90% CI 커버리지,
  Cook–Gelman–Rubin, 이상값 ≈0.90)

### 2.5 MH 확장성 한계 — 원논문 componentwise acMH

원논문의 acMH는 한 sweep에서 **n개 성분을 하나씩** MH 갱신하고, 성분마다 log-posterior
(‖B(θ−u₀)‖² 포함)를 2회 평가한다 → sweep당 **≈O(n²)**. 실측(μ±1.2, α0.35, `acmh_scale.py`):

| n | 실측 sec/sweep | T=5000 총시간 (1체인·1시드) |
|---|---|---|
| 100 | 0.003 | 15초 |
| 400 | 0.031 | 2.5분 |
| 1600 | 0.446 | 37분 |
| **2000** (투영) | 0.52 | **43분** |
| **5000** (투영) | 2.58 | **3.5시간** |
| **10000** (투영) | 8.70 | **12시간** |

스케일 지수 실측 **sec/sweep ∝ n^1.76**. n=10000이면 1체인·1시드가 12시간 →
**5시드 × 4체인 = 약 10일**. SG-MCMC는 같은 조건에서 **~14초/시드**(런타임 §5). 따라서
**대형 n에서 componentwise acMH는 비용상 실행 불가**이며, 본 스터디의 대형 n MH는 블록
tMH로 대체한다(그마저 통계적으로 붕괴 — §4.1). 이것이 SG-MCMC를 쓰는 1차 동기다.

## 3. 샘플링 결과 (T=1)

### 3.1 기본 조건 — 대형 n 재설계 (μ±1.2, α=0.35, n=2000/5000/10000, 5시드)

> **왜 대형 n으로 재설계했나** — 초기 기본은 n=100이었으나 너무 작아 모든 샘플러가 비슷하게
> 나와 변별이 안 됐다. 대형 n에서는 (1) 원논문 componentwise acMH가 **비용상 불가**(§2.5),
> (2) 블록 tMH·SGLD도 **통계적으로 붕괴**(§4.1)해 SG-MCMC(특히 AWSGLD)의 확장성 우위가
> 드러난다. 대형 n에선 acMH를 못 돌리므로 MH는 블록 tMH로 대체한다. 아래는 n=5000
> 대표값이며 n=2000·10000도 방향이 동일하다.

**표1 — γ별 FDR 제어** (n=5000, precision / recall / F1 / realFDR)

| γ | method | precision | recall | F1 | realFDR |
|---|---|---|---|---|---|
| 0.10 | qSGLD | 0.986±0.004 | 0.155±0.014 | 0.268±0.021 | 0.014±0.004 |
| 0.10 | **AWSGLD** | 0.976±0.009 | **0.185±0.028** | **0.310±0.038** | 0.024±0.009 |
| 0.10 | SGHMC | 0.899±0.064 | 0.007±0.002 | 0.014±0.004 | 0.101±0.064 |
| 0.10 | SGLD | 0.729±0.064 | 0.004±0.002 | 0.008±0.004 | 0.271±0.064 |
| 0.20 | qSGLD | 0.867±0.010 | 0.714±0.020 | **0.783±0.010** | 0.133±0.010 |
| 0.20 | **AWSGLD** | 0.855±0.010 | **0.716±0.023** | 0.779±0.012 | 0.145±0.010 |
| 0.20 | SGHMC | 0.847±0.016 | 0.196±0.007 | 0.318±0.008 | 0.153±0.016 |
| 0.20 | SGLD | 0.669±0.019 | 0.093±0.004 | 0.163±0.006 | 0.331±0.019 |

realFDR이 목표 γ 안에 드는(통제되는) 것은 **qSGLD·AWSGLD**다. SGLD는 붕괴(recall≈0),
cycSGLD는 π가 한 점에 몰려 γ컷이 극단(0 또는 전체 선택). SGHMC는 보수적이라 recall이 낮다.

**표2 — 순위** (n=5000, Spearman / Kendall / Top-k / NDCG / AUC)

| method | Spearman | Kendall | Top-k | NDCG | AUC |
|---|---|---|---|---|---|
| qSGLD | 0.734±0.012 | 0.522±0.010 | **0.820±0.006** | 0.839±0.006 | 0.886±0.008 |
| **AWSGLD** | 0.717±0.013 | 0.507±0.011 | 0.813±0.008 | 0.832±0.007 | 0.877±0.008 |
| cycSGLD | 0.687±0.022 | 0.449±0.024 | 0.784±0.008 | 0.814±0.007 | 0.850±0.012 |
| SGHMC | 0.416±0.013 | 0.280±0.009 | 0.695±0.006 | 0.713±0.005 | 0.718±0.009 |
| SGLD | 0.128±0.007 | 0.085±0.004 | 0.594±0.003 | 0.602±0.002 | 0.569±0.006 |

AWSGLD ≈ qSGLD(사실상 동률)로 최상위 tier.

**표3 — σ̂ / Jaccard / cov90** (n=5000, gold-free·θ*)

| method | σ̂ | Jaccard | cov90 |
|---|---|---|---|
| **AWSGLD** | 1.27±0.02 | 0.555±0.003 | **0.974±0.003** |
| qSGLD | 1.28±0.01 | 0.558±0.004 | 0.971±0.003 |
| SGHMC | 1.08±0.01 | 0.449±0.006 | 0.886±0.008 |
| SGLD | 1.10±0.01 | 0.392±0.004 | 0.773±0.002 |
| cycSGLD | **0.05±0.00** | 0.978±0.002 | **0.052±0.003** |

**cycSGLD는 붕괴다.** σ̂≈0.05(사후폭 사실상 0), cov90≈0.05(90%CI가 참값을 거의 못 덮음).
Jaccard 0.98은 재현성이 아니라 **한 점에 붙어 매번 같은 답을 내는** 붕괴의 위장이다.
AWSGLD·qSGLD만 cov90≈0.97로 **보정(calibration)이 맞다.** n=10000에서도 동일(AWSGLD cov90
0.973 / cycSGLD 0.057). 대형 n에서 σ̂ 안정화를 위해 σ²·α Langevin 스텝을 1/n로 스케일했다.

### 3.2 난이도·n·문서수 스윕 (샘플링)

Easy(μ2.5,α0.1)/Moderate(μ1.8,α0.2)/Difficult(μ1.0,α0.4), n∈{100,300,1000,5000,10000}에서
같은 3표를 반복. 결론은 일정하다: **AWSGLD·qSGLD가 cov90·FDR 통제에서 최상위, 나머지는
붕괴하거나 보정 실패.** n=5000·10000에서는 σ²·α Langevin 스텝을 1/n로 스케일(step 안정화)해
발산을 막았다(초기 σ̂≈84 → 1.3).

> 초기의 `redesign_gpu_n300_*_k{3,10}.csv` 문서 수 스윕은 시뮬레이션 데이터에 문서 라벨을
> 무작위 배정(`doc=rng.integers(0,K,N)`)한 **합성** 버전이었다. 실제 문서로 병합한 올바른
> 버전은 §3.3(샘플링)·§4.5(최적화)로 대체했다.

## 3.3 문서 수 — 실제 Hulth 문서 병합 (표준 단봉, 샘플링)

**Study 3과 문서 소스는 같되 에너지가 다르다.** Study 3은 문서별 중심을 봉우리로 하는
log-sum-exp mixture(다봉)로 trap을 만든다. 여기서는 **병합 그래프 하나의 표준 에너지**
(u0 = 병합 TextRank, 단일 중심)를 써서 단봉을 유지한다. 문서: K=1[1994], K=3[1994,212,227],
K=10[Study 3의 10문서], n=76/118/322, truth=26/49/156, 5시드.

> 실데이터엔 데이터생성 θ가 없어 cov90·Spearman·MSE는 정의 불가(Study 3과 동일). 따라서
> 키워드 truth 기반(Top-k·NDCG·AUC·γ-FDR)과 gold-free(σ̂·Jaccard)만 보고한다.

**Top-k / NDCG / AUC / σ̂ / Jaccard** (5시드 mean±std)

| K | method | Top-k | NDCG | AUC | σ̂ | Jaccard |
|---|---|---|---|---|---|---|
| 3 | **tMH** | **0.706±0.033** | **0.775±0.025** | **0.809±0.031** | 2.58 | 0.550 |
| 3 | AWSGLD | 0.682±0.044 | 0.747±0.037 | 0.794±0.031 | 2.14 | 0.421 |
| 3 | qSGLD | 0.678±0.055 | 0.738±0.047 | 0.792±0.033 | 1.98 | 0.410 |
| 3 | SGHMC | 0.604±0.063 | 0.670±0.050 | 0.724±0.034 | 1.12 | 0.290 |
| 3 | cycSGLD | 0.633±0.000 | 0.700±0.001 | 0.743±0.002 | **0.05** | 0.966 |
| 3 | SGLD | 0.473±0.030 | 0.496±0.043 | 0.598±0.016 | 1.09 | 0.263 |
| 10 | **tMH** | **0.722±0.022** | **0.765±0.018** | **0.797±0.015** | 2.31 | 0.525 |
| 10 | AWSGLD | 0.709±0.017 | 0.755±0.016 | 0.776±0.028 | 2.10 | 0.497 |
| 10 | qSGLD | 0.712±0.017 | 0.755±0.016 | 0.780±0.019 | 2.03 | 0.490 |

**합성과 방향이 뒤집힌다.** 합성 실험(§3.1)에서는 AWSGLD·qSGLD가 tMH를 앞섰으나,
실문서 병합에서는 **tMH(원 논문 MH)가 순위 최상위**, AWSGLD≈qSGLD가 근소 2위다. 원인은
레짐 차이다: 실문서 병합은 키워드 밀도가 매우 높고(K=10에서 truth 156/322≈48%) α도 높아
(0.67~0.76) 순위 문제의 성격이 다르다. **표준 단봉에서 AWSGLD가 qSGLD와 사실상 동률**이라는
이 스터디의 핵심 결론은 실데이터에서도 유지된다(Top-k 차 <0.01).

**γ별 FDR (γ=0.2 발췌)** — 병합 문서의 truth 밀도(≈48%)가 높아 realFDR이 목표를 크게 초과.

| K | method | precision | recall | F1 | realFDR |
|---|---|---|---|---|---|
| 10 | AWSGLD | 0.556 | 0.914 | 0.690 | 0.444 |
| 10 | qSGLD | 0.560 | 0.921 | 0.696 | 0.440 |
| 10 | tMH | 0.552 | 0.944 | 0.694 | 0.448 |
| 10 | SGHMC | 0.843 | 0.258 | 0.393 | 0.157 |
| 10 | cycSGLD | 0.704 | 0.513 | 0.593 | 0.296 |

realFDR이 γ=0.2를 크게 넘는다. 합성(§3.1)에서 AWSGLD·qSGLD가 FDR을 통제하던 것과 달리,
**실문서 병합에서는 어떤 샘플러도 명목 FDR을 못 지킨다.** truth가 노드의 절반이라
precision 상한 자체가 낮은 실데이터 병합의 구조적 한계다(순위·AUC로 봐야 함). cycSGLD는
여기서도 붕괴(σ̂≈0.05·Jaccard≈0.97).

## 4. 최적화 결과

### 4.1 annealing (T→0), 기본 조건 대형 n (μ±1.2, α=0.35, 5시드) — MH 붕괴

예산=AWSGLD Tε(2000~3000). opt.gap(U−U*) 기준:

| method | n=2000 | n=5000 | n=10000 |
|---|---|---|---|
| **AWSGLD** | **1.03±0.04** | **1.72±0.05** | **3.86±0.12** |
| qSGLD | 1.83±0.00 | 2.74±0.01 | 5.46±0.01 |
| cycSGLD | 2.25±0.22 | 4.86±0.34 | 9.67±0.40 |
| SGHMC | 28.69±3.50 | 60.46±3.17 | 119.75±4.83 |
| SGLD | 594.96±11.56 | 1395.06±8.58 | 2770.91±17.88 |
| tMH | **1783.84±8.44** | **5606.67±45.69** | **12063.48±59.37** |

**대형 n에서 MH는 최적화에 실패한다.** 블록 tMH의 gap이 수천~만 단위로 폭발(예산 안에
ε 근처도 못 감), SGLD도 붕괴. **AWSGLD가 전 n에서 단독 1위**(gap 1.03→3.86), qSGLD 2위.
gm(gradient multiplier)이 유한온도에서 살아 있어 qSGLD보다도 낮은 gap을 낸다. §2.5의
componentwise acMH는 이 규모에서 **아예 못 돌린다**(비용상 불가). 즉 대형 n은 MH 계열
전체(비용·통계 양쪽)가 무너지고 SG-MCMC만 남는 레짐이다.

### 4.2 annealing, 난이도별 (n=100, budget=AWSGLD Tε)

| 난이도 | AWSGLD gap | qSGLD gap | acMH gap | SGLD gap |
|---|---|---|---|---|
| Easy (μ2.5,α0.1) | **1.00±0.04** | 1.29±0.05 | 2.25±0.25 | 49.50±1.76 |
| Moderate (μ1.8,α0.2) | **1.00±0.03** | 1.29±0.04 | 2.25±0.19 | 45.60±2.96 |
| Difficult (μ1.0,α0.4) | **1.03±0.03** | 1.43±0.05 | 2.30±0.23 | 41.42±1.88 |

### 4.3 annealing, n 스윕 (Moderate, MH=tMH)

| n | AWSGLD gap | qSGLD gap | SGHMC gap | SGLD gap | tMH gap |
|---|---|---|---|---|---|
| 100 | **1.00±0.04** | 1.66±0.05 | 9.76±1.13 | 56.67±3.01 | 2.51±0.25 |
| 1000 | **1.03±0.04** | 1.79±0.01 | 30.10±3.19 | 362.91±9.07 | 625.20±9.80 |
| 5000 | **1.75±0.09** | 2.76±0.01 | 128.69±1.58 | 1606.83±8.00 | 5861.45±50.02 |
| 10000 | **3.86±0.07** | 5.49±0.01 | 254.51±4.22 | 3183.73±17.95 | 12675.25±46.60 |

**대형 n에서 tMH·SGLD는 최적화 실패**(gap이 폭발). AWSGLD가 모든 n에서 최소 gap.

### 4.4 상수 온도 T=0.01 — gm 소멸 발견

annealing에서 AWSGLD의 우위는 gm에서 온다. gm은
`gm = 1 + (ζ·T/Δu)(log w_J − log w_{J−1})` 이므로 **T→0이면 gm→1**, AWSGLD는 qSGLD로
환원된다. 이를 상수 온도로 검증했다.

- **T=1e-8**: AWSGLD ≡ qSGLD (완전 동일).
- **T=0.01** (채택): AWSGLD ≈ qSGLD. n=100에선 gap 차 <0.02로 사실상 동률이나,
  **n=300 이상에서는 AWSGLD가 근소하게 앞선다**(난이도 3종 gap 1.16~1.17 vs qSGLD 1.19,
  ‖θ−θ*‖도 작음). 둘 다 최상위 tier로 cycSGLD/SGHMC/tMH/SGLD를 크게 앞섬.

**함의**: AWSGLD의 최적화 고유 우위는 **유한온도(annealing 초기 탐색)에서만** 발현된다.
저온 극한에서는 전처리 Langevin(qSGLD)과 같은 최소로 수렴한다. 이는 결함이 아니라
"AWSGLD는 본질적으로 유한온도 샘플링 알고리즘"이라는 정체성의 확인이다.

### 4.5 문서 수 — 실제 Hulth 문서 병합 (표준 단봉, 최적화 annealing)

§3.3과 같은 실문서 병합 에너지에서 최적화. 예산=AWSGLD Tε(~1300/1600/2000), 5시드.

| method | K=1 gap | K=3 gap | K=10 gap | K=10 ‖θ−θ*‖ |
|---|---|---|---|---|
| **AWSGLD** | **1.03** | **1.03** | **1.01** | **2.13** |
| qSGLD | 1.87 | 1.29 | 1.63 | 2.93 |
| tMH | 2.40 | 2.87 | 51.37 | 18.22 |
| cycSGLD | 2.76 | 3.15 | 6.54 | 12.47 |
| SGHMC | 6.88 | 8.88 | 19.18 | 18.01 |
| SGLD | 32.11 | 45.84 | 95.38 | 23.86 |

**최적화는 실문서에서도 AWSGLD가 모든 K에서 단독 1위**(gap 1.01~1.03로 거의 불변),
qSGLD 2위. tMH는 K=10에서 붕괴(gap 51)한다. 샘플링 순위(§3.3)에선 tMH가 앞섰지만
**최적화 축에서는 AWSGLD가 명확히 이긴다** — 두 축을 함께 봐야 하는 이유다.

## 5. 런타임 (알고리즘별 wall-clock)

환경: RTX PRO 6000 (CUDA), torch float64, dense matvec. **시드 1개 기준 초** (총 시간 =
표값 × 시드수). 순수 샘플러 루프 시간(초기화·데이터생성·U* 계산 제외).

**샘플링 (T=1, iters=5000: burn-in 2500, thin 5 → 표본 500)** — 초/시드

| n | tMH | SGLD | qSGLD | cycSGLD | SGHMC | AWSGLD |
|---|---|---|---|---|---|---|
| 100 | 1.13 | 0.52 | 0.57 | 0.51 | 0.54 | 1.50 |
| 1000 | 1.15 | 0.52 | 0.57 | 0.52 | 0.54 | 1.52 |
| 5000 | 3.16 | 1.07 | 2.50 | 1.06 | 1.08 | 4.15 |
| 10000 | 10.56 | 3.83 | 9.56 | 3.89 | 3.91 | 13.82 |

**최적화 (T→0, iters=2000)** — 초/시드

| n | tMH | SGLD | qSGLD | cycSGLD | SGHMC | AWSGLD |
|---|---|---|---|---|---|---|
| 100 | 0.44 | 0.21 | 0.23 | 0.21 | 0.22 | 0.59 |
| 1000 | 0.45 | 0.21 | 0.23 | 0.21 | 0.22 | 0.59 |
| 5000 | 1.25 | 0.42 | 1.00 | 0.42 | 0.43 | 1.64 |
| 10000 | 4.23 | 1.58 | 3.89 | 1.59 | 1.60 | 5.58 |

AWSGLD는 U(θ) 평가·가중 갱신이 추가돼 가장 느리지만(qSGLD의 ~2.6배), n=10000에서도
샘플링 14초/시드로 실용 범위다. tMH는 매 스텝 U 2회 평가로 SG-MCMC보다 무겁다.

## 6. 해석

- **핵심(확장성) — 대형 n에서 MH 계열이 무너지고 SG-MCMC만 남는다.** 원논문 componentwise
  acMH는 O(n²)라 n≥2000에서 비용상 불가(§2.5, n=10000 약 10일), 블록 tMH는 통계적으로
  붕괴(최적화 gap 수천~만, §4.1). n=100 소규모에선 안 보이던 이 격차가 n을 키우면 드러난다.
  **이것이 BSS에 SG-MCMC(AWSGLD)를 도입하는 1차 정당화다.**
- **표준 볼록 BSS에서 AWSGLD ≈ qSGLD.** 둘 다 전처리(BᵀB)⁻¹ Langevin이라 볼록 문제에서
  같은 곳으로 간다. 다봉 탈출(Study 3)이라는 AWSGLD 고유 무기는 여기서 쓸 데가 없다.
- **그럼에도 AWSGLD가 두 축 모두에서 근소 우위.** 최적화 annealing에서 단독 1위
  (gap 1.04 최소·Tε 최소), 샘플링에서 cov90 최고(0.978)·FDR 통제 하 recall/F1 최고.
  T=0.01 상수온도에서도 n≥300이면 qSGLD를 앞선다.
- **AWSGLD의 진짜 우위는 유한온도에 있다.** gm ∝ T이므로 최적화 극한(T→0)에서는 이점이
  사라지고, 유한온도 샘플링(cov90·FDR)과 annealing 초기 탐색에서 발현된다.
- **cycSGLD의 높은 Jaccard는 함정.** 재현성이 좋아 보이지만 붕괴(σ̂≈0, cov90≈0.05)의
  위장이다. gold-free 지표는 반드시 σ̂·cov90와 함께 읽어야 한다.
- **실문서 병합에서는 축이 갈린다(§3.3·4.5).** 최적화는 실문서에서도 AWSGLD가 모든 K에서
  1위지만, 샘플링 순위는 tMH(원 논문 MH)가 앞서고 AWSGLD≈qSGLD가 근소 2위다. 합성에서
  AWSGLD/qSGLD가 tMH를 앞선 것과 뒤집힌다 — 실문서 병합은 키워드 밀도(≈48%)·α가 높은
  다른 레짐이기 때문. **"표준 단봉에서 AWSGLD≈qSGLD"라는 결론은 합성·실데이터 모두에서
  유지**되나, tMH 대비 우위는 데이터에 의존한다. 실데이터 FDR은 truth 밀도가 높아 어떤
  샘플러도 명목값을 못 지키므로 순위·AUC로 판단해야 한다.
- **2축 통합 서사** — "최적화가 목표"라는 요구와 "BSS는 불확실성이 강점"이라는 사실은
  온도 하나로 화해한다. 같은 데이터·에너지에서 T→0이면 MAP 최적화, T=1이면 사후 샘플.
  AWSGLD는 최적화 축에서 합성·실데이터 모두 최상위다. (다봉 탈출 우위는 Study 3의 몫으로 분리.)

## 7. 재현

```bash
cd simulation/study_fav

# 샘플링 (T=1) — 조건별 3표
python3 redesign_gpu.py <nseed> <n> <mu> <alpha> <zeta> [KDOC] [sparse]
python3 aggregate_redesign.py "redesign_gpu_n100_mu1.2_a0.35_z1.0.csv"

# 최적화 (annealing / 상수 T)
python3 opt_convergence.py <nseed> <n> <mu> <alpha> [budget] [eps]     # CPU
python3 opt_convergence_gpu.py <NSEED> <N> <MU> <ALPHA> [KDOC] [EPS]   # GPU 대형 n
#   TCONST=0.01 python3 opt_convergence_gpu.py ...   # 상수 온도
#   BFIX=2999   python3 opt_convergence_gpu.py ...   # 예산 고정
python3 opt_by_difficulty.py <nseed>                                  # 난이도 3종
python3 docreal_gpu.py <K> [nseed=5]                                  # 실문서 1/3/10 병합(최적화+샘플링)

# 런타임
python3 timing_gpu.py <n> [T_samp=5000] [budget=2000]
python3 acmh_scale.py                                                # 원논문 acMH 확장성 실측·투영
```

소요 시간(참고): 샘플링 n=100 10시드 ~15초, n=10000 1시드 ~14초. 최적화 n=100 예산1500 ~1초.

## 8. 산출물

| 파일 | 내용 |
|---|---|
| `redesign_gpu.py` | 샘플링(T=1) 엔진 — 6샘플러 3표 지표 |
| `redesign_gpu_n{2000,5000,10000}_mu1.2_*.csv` | **기본 대형 n 재설계** 샘플링 (**§3.1**) |
| `redesign_gpu_n{100,300,1000,5000,10000}_*.csv` | 조건별 샘플링 결과 (난이도·n·문서수 스윕) |
| `acmh_scale.py` | 원논문 componentwise acMH 확장성 실측·투영 (**§2.5**) |
| `docreal_gpu.py` | **실제 Hulth 문서 1/3/10개 병합**(표준 단봉) — 최적화+샘플링 (**본 문서 §3.3·4.5**) |
| `docreal_opt_k{1,3,10}.csv` / `docreal_samp_k{1,3,10}.csv` | 실문서 문서 수 스윕 결과 |
| `redesign_gpu_n300_*_k{3,10}.csv` | (구) 합성 문서 라벨 배정 — `docreal_*`로 대체됨, 참고용 |
| `opt_convergence.py` / `opt_convergence_gpu.py` | 최적화 수렴 비교 (CPU annealing / GPU 대형 n·TCONST·BFIX) |
| `opt_by_difficulty.py` | 난이도별 최적화 |
| `opt_results_difficulty.csv` | 난이도 최적화 결과 (**본 문서 4.1~4.2**) |
| `opt_results_nsweep_moderate.csv` | n 스윕 최적화 결과 (**본 문서 4.3**) |
| `aggregate_redesign.py` | 샘플링 3표 집계 |
| `timing_gpu.py` | 알고리즘별 wall-clock 런타임 (**본 문서 5**) |
| `_archive/` | 폐기된 mixture/CPU/μ2.0 실험 보관 |

> **주의** — T=0.01 상수온도 최적화 결과(§4.4)와 런타임 표(§5)는 스크립트가 stdout으로만
> 출력하므로 Notion "재실험 결과" 페이지(T=0.01 토글·#5)가 유일한 갈무리 기록이다.
